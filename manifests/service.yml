apiVersion: v1
kind: Service
metadata:
    name: "backend-go-voting-svc"
    labels:
        app: "backend-go-voting-svc"
spec:
    type: LoadBalancer
    externalTrafficPolicy: Local
    ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "backend-go-voting"